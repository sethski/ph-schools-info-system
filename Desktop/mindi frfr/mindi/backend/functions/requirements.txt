# =============================================================================
# MINDI Phase 1 — Python ML Backend Requirements
# Python 3.11+
# =============================================================================

# Web framework
fastapi==0.110.3
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# MongoDB
pymongo[srv]==4.8.0

# OpenRouter / HTTP client
httpx==0.27.0
aiohttp==3.9.5

# ML — Embeddings (BGE-M3)
sentence-transformers==3.0.1
torch==2.3.1
transformers==4.41.2
onnxruntime==1.18.0
numpy==1.26.4

# PII Redaction
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
spacy==3.7.4
# After install: python -m spacy download en_core_web_sm

# NLP utilities
nltk==3.8.1
scikit-learn==1.5.0

# Zero-shot classifier (region tagging)
# Uses facebook/bart-large-mnli via transformers pipeline

# Semantic deduplication
faiss-cpu==1.8.0   # Local vector similarity for dedup

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==42.0.8

# Utilities
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.3.0
loguru==0.7.2
tenacity==8.3.0   # Retry logic for OpenRouter calls
